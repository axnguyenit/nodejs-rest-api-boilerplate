// import { setOpenAPIMetadata } from 'routing-controllers-openapi';
// import type { AppRole } from './role.enum';

// // eslint-disable-next-line @typescript-eslint/naming-convention
// export const Roles = (roles: Array<AppRole>) => setOpenAPIMetadata('roles', roles);

export {};
